import React from 'react';
import { connect } from 'react-redux';

const AgenciesScreen = (props) => {
  return(
    <div>
      <h2>Agencies</h2>
    </div>
  )
}

export default connect()(AgenciesScreen);
